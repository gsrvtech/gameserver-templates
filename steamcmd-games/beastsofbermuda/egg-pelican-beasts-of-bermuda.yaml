_comment: 'DO NOT EDIT: FILE GENERATED AUTOMATICALLY BY PANEL'
meta:
  version: PLCN_v3
  update_url: 'https://raw.githubusercontent.com/gsrvtech/gameserver-templates/main/steamcmd-games/beastsofbermuda/egg-pelican-beasts-of-bermuda.yaml'
exported_at: '2024-08-31T07:59:54+00:00'
name: Beasts of Bermuda
author: 'eggs@goover.dev'
uuid: 43e24ef6-8ebc-4e4a-9e7c-2ab741f87ff4
description: 'Beasts of Bermuda is a multiplayer game with large servers where you grow up as a dinosaur or other prehistoric creature. You become part of a living, player-created ecosystem. The ultimate goal is to survive on one of the islands within the world of Bermuda.'
icon: null
tags: {  }
features:
- steam_disk_space
docker_images:
  'ghcr.io/goover/steamcmd:debian': 'ghcr.io/goover/steamcmd:debian'
file_denylist: {  }
startup_commands:
  Default: './LinuxServer/BeastsOfBermuda/Binaries/Linux/BeastsOfBermudaServer -GameMode {{GAMEMODE}} -MapName {{MAP}} -SessionName {{SESSION_NAME}} -NumPlayers {{MAX_PLAYERS}} $(if [ -n "$SRV_PW" ]; then echo "-ServerPassword=\"${SRV_PW}\""; fi) ?Port={{SERVER_PORT}} ?QueryPort={{QUERY_PORT}} -log'
config:
  files: {  }
  startup:
    done: Finished loading server data
  logs: {  }
  stop: ^C
scripts:
  installation:
    script: |-
      #!/bin/bash
      # File: Pterodactyl BEast of Bermuda Egg
      # Authors: gOOvER
      # Date: 2024/03/30
      # License: AGPL License

      clear
      RED='\033[0;31m'
      GREEN='\033[0;32m'
      YELLOW='\033[1;33m'
      BLUE='\033[0;34m'
      NC='\033[0m'

      echo -e "${BLUE}-------------------------------------------------${NC}"
      echo -e "${YELLOW}Beast of Bermuda Installscript${NC}"
      echo -e "${YELLOW}Egg by gOOvER | https://goover.dev${NC}"
      echo -e "${BLUE}-------------------------------------------------${NC}"

      #apt -y update
      #apt -y --no-install-recommends install curl lib32gcc-s1 ca-certificates jq dos2unix

      echo -e "${BLUE}-------------------------------------------------${NC}"
      echo -e "${YELLOW}download and install steamcmd. please wait...${NC}"
      echo -e "${BLUE}-------------------------------------------------${NC}"

      ## just in case someone removed the defaults.
      if [[ "${STEAM_USER}" == "" ]] || [[ "${STEAM_PASS}" == "" ]]; then
          echo -e "steam user is not set.\n"
          echo -e "Using anonymous user.\n"
          STEAM_USER=anonymous
          STEAM_PASS=""
          STEAM_AUTH=""
      else
          echo -e "user set to ${STEAM_USER}"
      fi

      ## download and install steamcmd
      cd /tmp
      mkdir -p /mnt/server/steamcmd
      curl -sSL -o steamcmd.tar.gz https://steamcdn-a.akamaihd.net/client/installer/steamcmd_linux.tar.gz
      tar -xzvf steamcmd.tar.gz -C /mnt/server/steamcmd
      mkdir -p /mnt/server/steamapps # Fix steamcmd disk write error when this folder is missing
      cd /mnt/server/steamcmd

      # SteamCMD fails otherwise for some reason, even running as root.
      # This is changed at the end of the install process anyways.
      chown -R root:root /mnt
      export HOME=/mnt/server
      echo -e "${GREEN}..done..${NC}"

      ## install game using steamcmd
      echo -e "${BLUE}-------------------------------------------------${NC}"
      echo -e "${YELLOW}install game using steamcmd. please wait...${NC}"
      echo -e "${BLUE}-------------------------------------------------${NC}"

      ./steamcmd.sh +force_install_dir /mnt/server +login ${STEAM_USER} ${STEAM_PASS} ${STEAM_AUTH} $( [[ "${WINDOWS_INSTALL}" == "1" ]] && printf %s '+@sSteamCmdForcePlatformType windows' ) $( [[ "${STEAM_SDK}" == "1" ]] && printf %s '+app_update 1007' ) +app_update ${SRCDS_APPID} $( [[ -z ${SRCDS_BETAID} ]] || printf %s "-beta ${SRCDS_BETAID}" ) $( [[ -z ${SRCDS_BETAPASS} ]] || printf %s "-betapassword ${SRCDS_BETAPASS}" ) ${INSTALL_FLAGS} validate +quit
      echo -e "${GREEN}..done..${NC}"

      ## set up 32 bit libraries
      mkdir -p /mnt/server/.steam/sdk32
      cp -v linux32/steamclient.so ../.steam/sdk32/steamclient.so

      ## set up 64 bit libraries
      mkdir -p /mnt/server/.steam/sdk64
      cp -v linux64/steamclient.so ../.steam/sdk64/steamclient.so

      # add below your custom commands if needed

      FILE=$HOME/LinuxServer/BeastsOfBermuda/Saved/Config/LinuxServer/Game.ini

      if [ -f "$FILE" ]; then
        echo -e "${BLUE}-------------------------------------------------${NC}"
        echo -e "${GREEN}Game.ini found.${NC}"
        echo -e "${BLUE}-------------------------------------------------${NC}"
      else
        echo -e "${BLUE}-------------------------------------------------${NC}"
        echo -e "${RED}No Game.ini found. Downloading default...${NC}"
        echo -e "${BLUE}-------------------------------------------------${NC}"
        cd $HOME/LinuxServer/BeastsOfBermuda/Saved/Config/LinuxServer/
        curl -sSL -o Game.ini https://raw.githubusercontent.com/gsrvtech/gameserver-templates/main/steamcmd-games/beastsofbermuda/config/Game.ini
        echo -e "${GREEN}..done..${NC}"
      fi

      # Check and delete the WindowsServer file or directory
      if [ -e "/mnt/server/WindowsServer" ]; then
          echo -e "${BLUE}-------------------------------------------------${NC}"
          echo -e "${YELLOW}Found WindowsServer. Deleting...${NC}"
          echo -e "${BLUE}-------------------------------------------------${NC}"
          rm -rf /mnt/server/WindowsServer
          echo -e "${GREEN}..done..${NC}"
      else
          echo -e "${BLUE}-------------------------------------------------${NC}"
          echo -e "${GREEN}No WindowsServer found. Skipping deletion...${NC}"
          echo -e "${BLUE}-------------------------------------------------${NC}"
      fi

      ## install end
      echo -e "${BLUE}-------------------------------------------------${NC}"
      echo -e "${GREEN}Installation completed...${NC}"
      echo -e "${BLUE}-------------------------------------------------${NC}"
    container: 'ghcr.io/goover/installers:debian'
    entrypoint: bash
variables:
- name: SRCDS_APPID
  description: ''
  env_variable: SRCDS_APPID
  default_value: '882430'
  user_viewable: false
  user_editable: false
  rules: 'required|string'
  sort: 1
  field_type: text
- name: QueryPort
  description: ''
  env_variable: QUERY_PORT
  default_value: '27015'
  user_viewable: true
  user_editable: false
  rules: 'required|integer|between:1024,65536'
  sort: 2
  field_type: text
- name: Session Name
  description: 'NO SPACES ALLOWED !!!!'
  env_variable: SESSION_NAME
  default_value: goover.dev - Beast of Bermuda Egg
  user_viewable: true
  user_editable: true
  rules: 'required|string|max:50'
  sort: 3
  field_type: text
- name: Max Players
  description: ''
  env_variable: MAX_PLAYERS
  default_value: '32'
  user_viewable: true
  user_editable: true
  rules: 'required|integer|between:1,32'
  sort: 4
  field_type: text
- name: Server Password
  description: 'NO SPACES ALLOWED !!!!'
  env_variable: SRV_PW
  default_value: ''
  user_viewable: true
  user_editable: true
  rules: 'nullable|string'
  sort: 5
  field_type: text
- name: Gamemode
  description: ''
  env_variable: GAMEMODE
  default_value: Life_Cycle
  user_viewable: true
  user_editable: true
  rules: 'required|string|in:Life_Cycle,Combat,Free_Roam'
  sort: 6
  field_type: text
- name: Map
  description: ''
  env_variable: MAP
  default_value: Forest_Island
  user_viewable: true
  user_editable: true
  rules: 'required|string|in:Forest_Island,Test_Performance,Bloodbath,Ancestral_Plains,Rival_Shores'
  sort: 7
  field_type: text
- name: Additional game args
  description: ''
  env_variable: GAME_ARGS
  default_value: ''
  user_viewable: true
  user_editable: true
  rules: 'nullable|string'
  sort: 8
  field_type: text
- name: '[SERVER] Auto Update'
  description: ''
  env_variable: AUTO_UPDATE
  default_value: '1'
  user_viewable: true
  user_editable: true
  rules: 'required|boolean'
  sort: 9
  field_type: text
